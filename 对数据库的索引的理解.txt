索引可以提高查询速度，但是会影响增删改操作。

索引也是一张表，该表保存了主键与索引字段，并指向实体表的记录，建立索引会占用磁盘空间的索引文件。

索引根据索引列的数量分单列索引和组合索引。单列索引，即一个索引只包含单个列，一个表可以有多个单列索引，但这不是组合索引。组合索引，即一个索引包含多个列。
索引根据索引字段的约束分为普通索引、唯一索引和主键索引
普通索引：无任何约束
唯一索引：索引字段值必须唯一
主键索引：索引字段值唯一且不能为空，创建表时追加了主键约束即创建了主键索引