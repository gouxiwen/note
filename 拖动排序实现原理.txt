一、 前提条件
1.一个父容器
2.容器内用for循环渲染数据一系列子元素
3.子元素允许拖动
二、实现原理
1.容器内元素相对与容器绝对定位，初始位置使用translate设置
2.记录数据的原始index-oindex，存入dataset
3.拖动过程中用oindex查找当前操作的元素数据，比较拖动元素经过每个目标元素时的前后位置关系，实时更新目标元素的位置(translate)
并根据当前位置关系更新所有元素的当前的index
4.拖动完成后用当前的index进行排序后更新数据