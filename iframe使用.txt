违反了同源策略就会出现跨域问题，主要表现为以下三方面：
1.无法读取cookie、localStorage、indexDB
2.DOM无法获得
3.ajax请求无法发送
4.一个文档里的脚本还是不能调用在其他文档里方法和读取属性

解决方法：

1.不同子域下的两个页面
设置相同的domain后可以变为同域的效果
2.使用postmessage传递消息


子页面在跨域的情况下无法读取和设置自身域名下的cookie