vue动态组件component配合keepAlive组件可以实现缓存
如果如果一个组件被缓存，它的直接子节点及其所有子孙节点都将被缓存，他们的 activated 和 deactivated 生命周期钩子将被调用

但要注意特殊情况，当一个缓存的组件内部又使用了子动态组件，需要分情况看：
1. 假如这个子动态组件初始绑定了组件1，但是该动态组件本身没有切换，这时切换祖先组件可以触发该子动态组件绑定的组件1的activated 和 deactivated钩子

2.如果切换这个子动态组件绑定的组件1和组件2，则需要再次使用keepAlive包裹，否则切换过程组件1和组件2不会被缓存