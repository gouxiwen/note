配置了缓存策略的响应头，按照缓存策略走

特殊的强缓存-启发式缓存
没有配置任何缓存策略，但还是走了本地缓存，这就是浏览器的默认缓存（启发式缓存）。
缓存的默认行为（即对于没有 Cache-Control 的响应）不是简单的“不缓存”，而是根据所谓的“启发式缓存”进行隐式缓存。
HTTP 旨在尽可能多地缓存，因此即使没有给出 Cache-Control，如果满足某些条件，响应也会被存储和重用。这称为启发式缓存。
缓存时间
公式为：( Date - Last-Modified ) * 0.1
Date：当前请求的时间
Last-Modified：服务器中资源最后被修改的日期

实践中发现：
谷歌浏览器
对于html，不会进行启发式缓存，而是进行协商缓存，而js、css、图片会进行启发式缓存
其他浏览器没有专门观察，可能会有一些差别

参考：https://blog.csdn.net/qq_42205731/article/details/139055005
